server:
  port: 8001 # 随机端口

spring:
  cloud:
    nacos:
      config:
        enabled: true
        refresh-enabled: true
        name: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace: TylorWu
        server-addr: 120.48.19.183:8848
        username: nacos
        password: nacos
        file-extension: yml


